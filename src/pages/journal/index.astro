---
import PageLayout from '../../layouts/PageLayout.astro';
import { getCollection } from 'astro:content';

const POSTS_PER_PAGE = 10;

const allPosts = (await getCollection('journal')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

const url = new URL(Astro.request.url);
const currentPage = Math.max(1, parseInt(url.searchParams.get('page') || '1'));
const totalPages = Math.ceil(allPosts.length / POSTS_PER_PAGE);
const start = (currentPage - 1) * POSTS_PER_PAGE;
const posts = allPosts.slice(start, start + POSTS_PER_PAGE);
---

<PageLayout title="Journal — The Forktree Project" description="The Forktree Project story - follow our rewilding journey." headerVariant="dark">

  <section class="journal-header">
    <div class="container--narrow">
      <h2><strong>The Forktree Story</strong></h2>
      <p>The Forktree Project is a journey, the first steps of which were taken when the property was purchased in April 2019. The blog posts below tell the story of how this journey is unfolding.</p>
    </div>
  </section>

  <section class="journal-grid">
    <div class="container">
      {posts.length > 0 ? (
        <div class="posts-grid">
          {posts.map((post) => (
            <a href={`/journal/${post.id}`} class="post-card">
              <div class="post-card__image">
                {post.data.image ? (
                  <img src={post.data.image} alt={post.data.title} loading="lazy" />
                ) : (
                  <div class="post-card__placeholder"></div>
                )}
              </div>
              <div class="post-card__content">
                <h3 class="post-card__title">{post.data.title}</h3>
                <div class="post-card__meta">
                  {post.data.author && <span>{post.data.author}</span>}
                  {post.data.date && (
                    <time datetime={new Date(post.data.date).toISOString()}>
                      {new Date(post.data.date).toLocaleDateString('en-AU', {
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric',
                      })}
                    </time>
                  )}
                </div>
              </div>
            </a>
          ))}
        </div>
      ) : (
        <p class="journal-empty">Journal posts coming soon. Check back for updates on our rewilding journey.</p>
      )}

      {totalPages > 1 && (
        <nav class="pagination" aria-label="Journal pagination">
          {currentPage > 1 && (
            <a href={currentPage === 2 ? '/journal' : `/journal?page=${currentPage - 1}`} class="pagination__link">
              &larr; Newer
            </a>
          )}
          <span class="pagination__info">Page {currentPage} of {totalPages}</span>
          {currentPage < totalPages && (
            <a href={`/journal?page=${currentPage + 1}`} class="pagination__link">
              Older &rarr;
            </a>
          )}
        </nav>
      )}
    </div>
  </section>

</PageLayout>

<style>
  .journal-header {
    padding-top: calc(var(--header-height) + var(--space-lg));
    padding-bottom: var(--space-lg);
    text-align: center;
  }

  .journal-header h2 {
    font-family: var(--font-heading);
    font-size: clamp(1.1rem, 2vw, 1.5rem);
    font-weight: 400;
    margin-bottom: var(--space-sm);
    letter-spacing: 0.5px;
  }

  .journal-header p {
    color: var(--color-text-secondary);
    line-height: 1.7;
    font-size: 0.95rem;
  }

  .journal-grid {
    padding: 0 0 var(--space-xl);
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
  }

  @media (max-width: 640px) {
    .posts-grid {
      grid-template-columns: 1fr;
    }
  }

  .post-card {
    display: block;
    text-decoration: none;
    color: inherit;
    transition: opacity var(--transition-normal);
  }

  .post-card:hover {
    opacity: 0.8;
  }

  .post-card__image {
    aspect-ratio: 1;
    overflow: hidden;
    margin-bottom: var(--space-sm);
    background-color: #eee;
  }

  .post-card__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .post-card__placeholder {
    width: 100%;
    height: 100%;
    background-color: #ddd;
  }

  .post-card__title {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 0.5rem;
    line-height: 1.4;
  }

  .post-card__meta {
    font-size: 0.75rem;
    color: var(--color-text-secondary);
    display: flex;
    gap: 0.5rem;
    font-family: var(--font-heading);
    letter-spacing: 0.5px;
  }

  .post-card__meta span::after {
    content: '\00b7';
    margin-left: 0.5rem;
  }

  .journal-empty {
    text-align: center;
    color: var(--color-text-secondary);
    padding: var(--space-xl) 0;
  }

  .pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-lg) 0 0;
  }

  .pagination__link {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--color-text);
    transition: opacity var(--transition-normal);
  }

  .pagination__link:hover {
    opacity: 0.6;
  }

  .pagination__info {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    letter-spacing: 1px;
    color: var(--color-text-secondary);
  }
</style>
